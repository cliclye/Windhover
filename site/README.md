# Windhover download landing

Static page that links to the GitHub Releases DMG:

**https://github.com/cliclye/Kestrel/releases/latest/download/Windhover-macOS-arm64.dmg**

Open locally:

```bash
open site/index.html
```

Or serve from the repo root / this folder (e.g. GitHub Pages from `/site`).

The marketing CTA on the Vexilo site lives at `/windhover` in the sibling `vexiloWeb` repo and uses the same URL.
